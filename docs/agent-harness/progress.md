# Agent Progress

## Open question — unexplained acknowledgment without a delivered file

On 2026-08-12 at 21:05 a real transfer from `wirehop-cli` to the packaged macOS application returned `outcome confirmed`, which the sender only reports after receiving `{"ack":1}` — and the Qt receiver only sends that after committing every file. No file was ever found: not in `~/Downloads/WireHop`, not anywhere under `$HOME`, and not in Spotlight.

Evidence gathered since: the directory is writable and not blocked by TCC (a `wirehop-cli` receive into the same path succeeds), and adding a file does update the directory's mtime — which was still 21:05:17, so the file was not written and later removed. Attempts to reproduce failed at the response stage rather than the commit stage, because the sender's read timeout was too short for a human decision (fixed separately), so the anomaly itself was never re-observed.

Automated coverage of this exact path — the interop suite driving the real Qt receiver — passes on macOS and now on Linux CI. So this is either environment-specific or a rare path, not a broadly broken receive.

**Resolving it needs one manual accept**: send with `wirehop-cli send --port <app port> --name probe <file>`, accept in the dialog, and check `~/Downloads/WireHop`. Worth doing before this lands on master.

Use this file only for cross-session handoffs. Keep entries dated, factual, and linked to an active execution plan when one exists. Routine one-session changes do not need an entry.

- 2026-08-11: Initialized the repository-native agent harness. Documented the Qt/qmake architecture and security boundaries; added build, lint, startup-smoke, and intentionally unconfigured-test wrappers. Harness lint, a full macOS arm64 build with Qt 5.15.16/libsodium 1.0.20, and native startup smoke passed. `scripts/test.sh` intentionally returned status 2 because no automated suite exists. qmake warned that this Qt build was tested against an older macOS SDK than the local 15.2 SDK. No product source was changed.
- 2026-08-11: Completed `docs/exec-plans/completed/public-release-hardening.md` after committing the harness as `af7441e`. Added bounded transfer metadata, portable filename validation, temporary non-overwriting receives, protocol/crypto bounds, and a Qt Test suite in `2ddf46c`. All 24 tests, lint, a macOS arm64 build, and native startup smoke passed.
- 2026-08-11: Completed `docs/exec-plans/completed/wirehop-rebrand.md` in `de522fe`: WireHop 0.1.0 identity, original artwork, `io.github.tracyaniu.wirehop`, GitHub Releases update endpoint, one-time LANDrop settings migration. Cross-platform packaging repaired in `bf249bc` after a failed manual run; the Package workflow then succeeded. (Backfilled entry.)
- 2026-08-12: Completed `docs/exec-plans/completed/rust-core-m1-discovery.md` (M1a). Discovery datagrams are now specified in `PROTOCOL.md`, extracted out of `DiscoveryService` into the GUI-free `Protocol` module (they had never been testable — `discoveryservice.cpp` pulls in `QMessageBox`), reimplemented clean-room in Rust, and pinned by 3 construction vectors plus a 19-case parsing table both suites verify. `core` gained a `PeerTable` with last-seen expiry — the Qt baseline still keeps a peer until it announces `port: 0`, so a device that loses power is listed forever — and a blocking UDP `DiscoveryService` that answers requests unicast and can warm-start from remembered addresses. The gate caught a layering divergence: the 4096-byte bound sat in Qt's socket layer but Rust's parser; now enforced in both parsers. 63 Qt + 64 Rust cases green. Deferred to M1b: mDNS/multicast channels, network-change socket rebuild, IPv6 scope. Not verified: two-peer LAN discovery.
- 2026-08-12: Completed `docs/exec-plans/completed/rust-core-m0-foundation.md` on branch `protocol-versioning`. Started the Rust-core program (`docs/decisions/2026-08-12-rust-core-architecture.md`): `core/` is now a clean-room Rust implementation of wire protocol v1, held to the C++/Qt baseline by two mechanical gates — conformance vectors both sides verify, and live two-directional interop transfers between the Qt session objects and the Rust process. 61 Qt + 49 Rust cases green with `WIREHOP_REQUIRE_RUST=1`. Writing the second implementation exposed three `PROTOCOL.md` ambiguities (session-code derivation, file-data framing, independent field degradation) and one real application defect: a rejection was followed by a spurious "remote host closed the connection" error that replaced the reason, invisible to the pure-Qt suite because only a promptly-closing peer triggers it. Next: M1 discovery reliability.
- 2026-08-12: Completed `docs/exec-plans/completed/protocol-versioning.md` on branch `protocol-versioning`. The prior session implemented it in a sandbox without a Qt toolchain, so it was committed with compile/test/smoke never executed; this session ran them on the developer machine (lint, typecheck, 48-test suite, smoke — all green) and closed the gap. An independent code review then found that the commit had silently invalidated its own `LegacyReceiver` test double (it inherits `respond()`, so it now advertises caps and no longer models a legacy peer), that `peerHasCap` was a raw peer claim rather than the negotiated intersection the plan promised, that the capability array's wire order was nondeterministic, and that its length bound counted UTF-16 units instead of UTF-8 bytes — all fixed. The review's headline claim that the 2 s ACK grace is a net regression was investigated and rejected on evidence: legacy receivers close on completion (so they never waited the old 10 s), and no released build acks without advertising caps. Remaining unverified: real two-machine transfer, non-macOS runtime, interop with an actual LANDrop binary.
- 2026-08-12: Completed `docs/exec-plans/completed/release-hardening-2.md` on branch `release-hardening-2`: Qt6-safe multi-select removal; macOS smoke recovered (root cause was a macdeployqt-polluted `build-agent/`, now guarded in `_common.sh`); ad-hoc signed/verified macOS packaging via `scripts/package-macos.sh`; SendToDialog socket lifecycle; discovery datagram validation; session inactivity watchdog + 8-session cap (manually timed: idle abort at 30.2s, 9th connection refused); GUI-free loopback test suite (suite now 35 cases); CI test job gating packaging (verified green); additive LANDrop-compatible delivery ACK. Remaining: no Developer ID/notarization, discovery unauthenticated by design, no protocol version field.
